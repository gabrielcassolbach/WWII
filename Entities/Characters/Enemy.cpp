#include "Enemy.hpp"

/*CONSTRUCTORS & DESTRUCTORS*/
Enemy::Enemy(int ident, double px, double py, double sx, double sy, double vx, double vy, int hp):
Character(ident, px, py, sx, sy, vx, vy, hp)
{

}
Enemy::~Enemy(){

}